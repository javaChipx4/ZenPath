<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zenpath">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZenPath">

        <!-- ===== OTHER ACTIVITIES ===== -->
        <activity android:name=".HistoryActivity" android:exported="false"/>
        <activity android:name=".ActivityDiary" android:exported="false"/>
        <activity android:name=".DiaryHistoryActivity" android:exported="false"/>
        <activity android:name=".DiaryEntryReadActivity" android:exported="false"/>
        <activity android:name=".MoodActivity" android:exported="false"/>
        <activity android:name=".MoodHistoryActivity" android:exported="false"/>
        <activity android:name=".StressHistoryActivity" android:exported="false"/>
        <activity android:name=".SelectionGamesActivity" android:exported="false"/>
        <activity android:name=".PlanetActivity" android:exported="false"/>
        <activity android:name=".LanternReleaseActivity" android:exported="false"/>
        <activity android:name=".StarSweepIntroActivity" android:exported="false"/>
        <activity android:name=".StarSweepActivity" android:exported="false"/>
        <activity android:name=".MainActivity" android:exported="false"/>
        <activity android:name=".AddUserActivity" android:exported="false"/>

        <!-- ===== LAUNCHER (ONLY ONE) ===== -->
        <activity
            android:name=".HomeLoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- ===== LOGIN SCREEN ===== -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"/>

    </application>

</manifest>