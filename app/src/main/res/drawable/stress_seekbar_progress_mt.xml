<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- full track -->
    <item android:id="@android:id/background"
        android:drawable="@drawable/bg_stress_track_mt"/>

    <!-- filled progress -->
    <item android:id="@android:id/progress">
        <clip android:drawable="@drawable/stress_track_fil_mt" android:clipOrientation="horizontal"/>
    </item>

</layer-list>