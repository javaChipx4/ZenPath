<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />

    <!-- grid line -->
    <stroke
        android:width="1dp"
        android:color="#2A2A2A" />

    <!-- tiny padding inside cell so numbers don't touch border -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" />
</shape>